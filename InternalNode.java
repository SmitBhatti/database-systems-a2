import java.util.ArrayList;

public class InternalNode {

    private ArrayList<InternalNode> InternalNodes = new ArrayList<>();

    public ArrayList<InternalNode> getInternalNodes()
    {
        return InternalNodes;
    }
}